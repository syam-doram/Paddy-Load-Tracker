import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.local.paddyloadtracker',
  appName: 'PaddyLoadTracker',
  webDir: 'dist'
};

export default config;
